import React from 'react';

// Definimos un componente funcional llamado "Alumno"
function Alumno() {

  // Retornamos un elemento "div" que contiene un encabezado "h2" con el texto "Alumno"
  return (
    <div>
      <h2>Alumno</h2> {/* Este es el encabezado */}
    </div>
  );
}

// Exportamos el componente para que pueda ser utilizado en otros archivos
export default Alumno;

/*En resumen, el código define un componente funcional llamado "Alumno" 
que retorna un elemento HTML que contiene un encabezado con el texto "Alumno". 
Luego, este componente es exportado para que pueda ser utilizado en otros archivos.*/