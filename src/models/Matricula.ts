export interface Matricula {
  id: number; // Identificador de la matrícula
  idAlumno: number; // Identificador del alumno inscripto
  idMateria: number; // Identificador de la materia a la que se inscribió el alumno
  fecha: Date; // Fecha en la que se realizó la matrícula
}
